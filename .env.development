# 默认 mock：true；联调时改为 false
# 文件名规则：
# .env → 所有环境通用变量。
# .env.development → 仅开发环境生效。
# .env.production → 仅生产环境生效。
# 是否启用 mock
VITE_USE_MOCK=false

# 方案 A（Vite 代理，推荐）：保持 /api
# 生产环境可在 /.env.production 里设：
# VITE_USE_MOCK=false 且 VITE_BASE_API=/api（由网关/Nginx 反代到后端）。


VITE_BASE_API=/api

# 方案 B（直连后端 + CORS）：注释上面一行，启用下面一行
# VITE_BASE_API=http://localhost:8080/api
